modmap:
  - name: Ctrl or ESC
    remap:
      CapsLock:
        alone: ESC
        held: Ctrl_L
        alone_timeout_millis: 200
  - name: Shift or ESC
    remap:
      SHIFT_R:
        alone: ESC
        held: SHIFT_R
        alone_timeout_millis: 200
