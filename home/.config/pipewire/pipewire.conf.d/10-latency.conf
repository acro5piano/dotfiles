# Lower latency configuration
# quantum 480 @ 48kHz = 10ms (matches RNNoise frame size)

context.properties = {
    default.clock.quantum = 480
    default.clock.min-quantum = 32
    default.clock.max-quantum = 512
}
