---
name: brave-search
description: Search the web with Brave Search via the bx CLI. Use when current web info, docs, APIs, errors, releases, or facts are needed.
---

# Brave Search

Use the bash tool to run:

```bash
bx '<query>'
```

Keep queries focused. Read the JSON output and cite result titles/URLs when useful.

Requires `BRAVE_SEARCH_API_KEY` or `bx config set-key <key>`.
