#!/bin/bash

set -ue

for site in `cat ~/.junkairc`; do
  echo ''
  echo ''
  echo 'Move on to the next site? [Y/n]'
  echo "  $site"
  read -n 1 key
  if [ "$key" = 'n' ]; then
    continue
  fi
  brave $site 2>/dev/null
done
