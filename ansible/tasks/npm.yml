- name: Install packages with npm
  # become: "{{ os != 'mac' | default(true) }}"
  become: false
  community.general.npm:
    name: "{{ item }}"
    state: latest
    global: yes
  loop:
    - yarn
    - prettier
    - plantview
    - wscat
    - serve
    - sql-formatter
