#!/bin/bash


xhost +local:
docker run -it --rm \
       -v /tmp:/tmp \
       -v $HOME/.dotfiles/etc/debian-spacemacs:/home/emacs \
       -v $HOME:/home/emacs/host_home \
       -e DISPLAY=$DISPLAY \
       --user 1000 \
       emacs

xhost -local:
