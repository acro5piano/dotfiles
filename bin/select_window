#!/bin/bash

target=`wmctrl -l | perl -pe 's/  / /g' | cut -d ' ' -f 4- | perl -pe 's;^\s*N/A\s;;g' | \
        dmenu -i -b -l 30 -p '>'`

if [[ "$target" =~ ^$ ]]; then
   exit 1
else
    wmctrl -a "$target"
fi
