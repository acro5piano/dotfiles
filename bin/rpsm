#!/bin/bash

sudo modprobe -r psmouse
sudo modprobe psmouse

sleep 3

synclient TapButton1=1; synclient TapButton2=3; synclient TapButton3=2; synclient HorizTwoFingerScroll=1; synclient VertScrollDelta=-111; synclient HorizScrollDelta=-111;
