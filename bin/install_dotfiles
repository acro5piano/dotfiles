#!/bin/bash

DOTPATH=~/.dotfiles

for f in .??*
do
    [ "$f" = ".git" ] && continue

    if [ -e "$HOME/$f" ]; then
        rm -r "$HOME/$f"
    fi

    ln -snfv "$DOTPATH/$f" "$HOME/$f"
done

ln -snfv "$DOTPATH/bin" "$HOME/bin"

chmod 0700 "$HOME/.screen"

exec $SHELL -l

