#!/bin/bash

DOTPATH=~/.dotfiles

for f in $DOTPATH/.??*
do
    [ "$f" = ".git" ] && continue

    if [ -e "$HOME/$f" ]; then
        rm -rf "$HOME/$f"
    fi

    ln -snfv "$DOTPATH/$f" "$HOME/$f"
done

ln -snfv "$DOTPATH/bin" "$HOME/bin"

exec $SHELL -l

