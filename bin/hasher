#!/bin/bash

read -s -p '🔑 ' password

t=`echo "${password}__${1}" | shasum -a 256 | base64 | cut -c1-32`

echo $t

if [ `uname` == 'Linux' ]; then
    echo $t | xclip -i -selection clipboard
else
    echo $t | pbcopy
fi
