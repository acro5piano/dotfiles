#!/bin/bash

read -s -p '🔑 ' password

t=`echo "${password}__${1}" | shasum -a 256 | base64 | head -1 | cut -c1-32`

echo `echo $t | cut -c1-8`...

if [ `uname` == 'Linux' ]; then
    echo $t | xclip -i -selection clipboard
else
    echo $t | pbcopy
fi
