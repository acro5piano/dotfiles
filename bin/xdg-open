#!/bin/bash

for target_file in $@; do
    ext="${target_file##*.}"
    case $ext in
        'xlsx') libreoffice --calc $target_file ;;
        'txt') e $target_file;;
        'html') chromium-browser $target_file;;
        *) /usr/bin/xdg-open $target_file;;
    esac
done

